[package]
name = "restic-ignore"
version = "0.1.0"
edition = "2021"

[dependencies]
lexopt = "0.3"

[dev-dependencies]
tempfile = "3"

[profile.release]
strip = true
lto = true
opt-level = "z"
panic = "abort"
